import { describe, expect, it } from 'vitest';

describe('vitest harness', () => {
  it('fails intentionally to prove the test runner is wired up — delete this test once confirmed', () => {
    expect(true).toBe(false);
  });
});
