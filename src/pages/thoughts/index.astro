---
import { getCollection } from 'astro:content';
import BaseLayout from '@layouts/BaseLayout.astro';
import { formatDateForDisplay } from '@lib/date-formatter';

const entries = await getCollection('thoughts');
entries.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout
  title="Thoughts — stivaros"
  description="Technical notes, observations, and short essays."
>
  <header class="mb-10">
    <h1 class="text-3xl font-bold text-body mb-2">Thoughts</h1>
    <p class="text-muted">Technical notes, observations, and short essays.</p>
  </header>

  {
    entries.length === 0 ? (
      <p class="text-muted">No posts yet.</p>
    ) : (
      <ul class="link-fill space-y-8">
        {entries.map((entry) => (
          <li>
            <a href={`/thoughts/${entry.id}`} class="text-lg font-medium">
              {entry.data.title}
            </a>
            <div class="mt-1 flex items-baseline gap-2 text-sm text-muted">
              <time datetime={entry.data.date.toISOString()} class="tabular-nums shrink-0">
                {formatDateForDisplay(entry.data.date)}
              </time>
              {entry.data.description && (
                <>
                  <span aria-hidden="true">·</span>
                  <span>{entry.data.description}</span>
                </>
              )}
            </div>
          </li>
        ))}
      </ul>
    )
  }
</BaseLayout>
